#include <iostream>
#include <string>

int
main(void)
{
	char a, b, c, d, e;
	float f, g, h;
	int i, j, k, l, m, n;
    long long o, p, q;

    int arr[5];

	std::cin>> i >> j >> k >> l>> m;

    std::cout << i * j - k * l * m;


	return 0;
}
